fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.24.0
python-dotenv>=1.0.0
jinja2>=3.1.0
pydicom>=2.4.0
langchain>=0.1.0
langchain-community>=0.0.10
chromadb>=0.4.22
faster-whisper>=1.0.0
python-multipart>=0.0.6
cryptography>=41.0.0
langchain-ollama>=0.1.0

# ── BioMedCLIP — etiquetado y prompt engineering ─────────────────────────
# En Jetson: torch y torchvision deben ser los builds JetPack ARM con CUDA.
# Instalar antes: https://developer.nvidia.com/embedded/jetpack
# luego: pip install open_clip_torch Pillow
open_clip_torch>=2.24.0
Pillow>=10.0.0

# ── Enrollment Biométrico ──────────────────────────────────────────────────
# PaddleOCR: en Jetson instalar primero paddlepaddle-gpu (build ARM)
# Ver: https://www.paddlepaddle.org.cn/install/quick (seleccionar JetPack)
paddleocr>=2.7.0
opencv-python-headless>=4.8.0
# InsightFace + ONNX Runtime (en Jetson usar onnxruntime-gpu build ARM)
insightface>=0.7.3
onnxruntime>=1.16.0
